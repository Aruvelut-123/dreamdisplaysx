package com.dreamdisplayx.platform.server.listeners

/**
 * Vanilla remote-control item support is intentionally kept behind a version-specific adapter.
 * The 1.21.x mappings used by this project do not expose one stable item-component API across all
 * supported loaders, so Paper support is enabled first and loader adapters remain disabled here.
 */
